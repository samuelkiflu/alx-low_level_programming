#include "main.h"

/**
 * mul - recives int a and b and multiplies them.
 * @a: first int parameter
 * @b: second int parameter
 * Return: multiple of a and b.
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
